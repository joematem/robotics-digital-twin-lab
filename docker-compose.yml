services:
  gpu-test:
    build:
      context: .
      dockerfile: docker/Dockerfile.gpu-test
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
